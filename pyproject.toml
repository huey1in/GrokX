[project]
name = "grok-reg-protocol"
version = "1.0.0"
description = "Browser-free Grok protocol registration CLI"
requires-python = ">=3.11"
dependencies = [
    "curl_cffi>=0.7",
]

[tool.uv]
package = false
